import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { UkanPage } from './ukan';

@NgModule({
  declarations: [
    UkanPage,
  ],
  imports: [
    IonicPageModule.forChild(UkanPage),
  ],
})
export class UkanPageModule {}
